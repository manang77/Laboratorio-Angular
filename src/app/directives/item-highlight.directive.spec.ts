import { ItemHighlightDirective } from './item-highlight.directive';

describe('ItemHighlightDirective', () => {
  it('should create an instance', () => {
    const directive = new ItemHighlightDirective();
    expect(directive).toBeTruthy();
  });
});
