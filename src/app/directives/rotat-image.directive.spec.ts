import { RotatImageDirective } from './rotat-image.directive';

describe('RotatImageDirective', () => {
  it('should create an instance', () => {
    const directive = new RotatImageDirective();
    expect(directive).toBeTruthy();
  });
});
